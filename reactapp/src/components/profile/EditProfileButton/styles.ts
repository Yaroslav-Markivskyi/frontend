import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  editButton: {
    textTransform: "none",
    fontSize: "16px",
    padding: "8px 16px",
    borderRadius: "8px",
    fontWeight: "bold",
  },
});

export default useStyles;
