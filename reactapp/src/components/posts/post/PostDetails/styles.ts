import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  postDetails: {
    padding: '16px',
    backgroundColor: '#fff',
    
  },
  description: {
    marginBottom: '12px',
    color: '#555',
  },
  price: {
    fontWeight: 'bold',
    color: '#28a745',
    textAlign: 'center',
  },
});

export default useStyles;
